[
  {
    "id": "20260713-phase1-design-lock",
    "timestamp": "2026-07-13T00:00:00Z",
    "concern": "design",
    "tags": ["gameplay-loop", "milestone", "drinking-game"],
    "milestone": "Define Core Loop",
    "files_affected": [".context/game_design.md", ".context/roadmap.md", ".context/todo_random.md", ".context/captains_log.json"],
    "summary": {
      "decision_made": "Locked Phase 1 gameplay loop for the pure-chance drinking-game race. Single round = Lobby → Bidding → Setup → Race → Settlement → Distribution → Ready. 9 phases total. Bidding capped 1–5, all bids public, 30s distribution timer with uniform-random fallback, 4 horses, track 6–20.",
      "options_considered": ["Pure-chance (chosen) vs. light influence lever for V1", "Full hidden bids vs. public bids (chose public for social drama)", "Bid cap 1–5 (chosen) vs. free-amount vs. multi-horse split", "Random fallback policy: uniform random (chosen) vs. equal split vs. lowest-consumption target"],
      "difficulties_encountered": "Track-card flip rule (last-placed horse triggers) creates an asymmetric early/late-race dynamic; explicitly documented in Core Loop Step 5 and Glossary 'Step (last-placed horse triggers flip)' to avoid future confusion.",
      "reversals_and_refactors": "N/A — first design pass.",
      "key_takeaway": "V1 is a pure-luck betting game where the only meaningful choice is bid-amount + post-race drinks distribution; deck-builder effect cards are explicitly deferred to V2."
    },
    "meta": {
      "confidence_score": 4
    }
  },
  {
    "id": "20260713-race-modifier-vault",
    "timestamp": "2026-07-13T12:00:00Z",
    "concern": "design",
    "tags": ["gameplay-loop"],
    "milestone": "Define Core Loop",
    "files_affected": [
      ".context/race_modifiers.md",
      "AGENTS.md",
      ".context/todo_random.md"
    ],
    "summary": {
      "decision_made": "Created a dedicated Future Concept Vault at .context/race_modifiers.md to capture expansion ideas (Energy Channeling, Track modifiers, Suit Synchronization) without scope-creeping the V1 prototype. AGENTS.md updated with a structured interview protocol for capturing and refining future modifier pitches.",
      "options_considered": [
        "Dedicated vault file with structured schema (field descriptions, beneficiary, upgrades) — chosen",
        "Keep ideas flattened in todo_random.md — rejected (too unstructured, no upgrade paths)",
        "No formal capture, rely on memory — rejected (ideas get lost)"
      ],
      "difficulties_encountered": "Balancing a schema structured enough for future design work but lightweight enough not to create friction during brainstorming. The current schema uses a 'fast-track' bypass clause for half-baked ideas.",
      "reversals_and_refactors": "N/A — first implementation of this process.",
      "key_takeaway": "Formalizing an ideation vault early prevents promising expansion concepts from either being forgotten or prematurely derailing the core prototype."
    },
    "meta": {
      "confidence_score": 3
    }
  },
  {
    "id": "20260713-architecture-selection",
    "timestamp": "2026-07-13T14:00:00Z",
    "concern": "architecture",
    "tags": ["networking", "state-machine", "hosting", "milestone"],
    "milestone": "Select Architecture Stack",
    "files_affected": [".context/roadmap.md", ".context/game_design.md", ".context/architecture.md"],
    "summary": {
      "decision_made": "Selected Cloudflare Workers + Durable Objects + Pages for the V1 prototype. Per-room Durable Objects provide strong state consistency for the 9-phase state machine with concurrent player actions. WebSocket transport via DO's Hibernation API handles real-time sync for up to 20 players per room. No database needed — game state is ephemeral in DO memory. Room codes derived from DO IDs.",
      "options_considered": [
        "Cloudflare Workers + Durable Objects + Pages (chosen) — true $0, per-room consistency, WebSocket native, edge-deployed, no DevOps",
        "Bun/Hono on Fly.io — simpler single-process model, free tier but VMs sleep on inactivity causing ~5s cold starts",
        "WebRTC P2P — pure peer-to-peer, no server cost, but NAT traversal and hosted-player model make it too complex for a prototype",
        "SSE + POST — works on serverless but clunky bidirectional flow for bidding/distribution phases"
      ],
      "difficulties_encountered": "Had to weigh per-room consistency guarantees against operational simplicity. The hosted player model (host manages bets/drinks for spectating players) eliminates WebRTC/P2P approaches because the server must be authoritative for both player types. Durable Objects' WebSocket Hibernation API solves the idle-room cost problem — inactive rooms cost nothing until a player reconnects.",
      "reversals_and_refactors": "N/A — first architectural decision.",
      "key_takeaway": "Durable Objects' per-room isolation maps cleanly to the game's room-per-game model; the 9-phase state machine becomes a simple class inside a DO, with each WebSocket connection representing one player (or one host managing multiple hosted players)."
    },
    "meta": {
      "confidence_score": 5
    }
  },
  {
    "id": "20260713-repo-layout-decision",
    "timestamp": "2026-07-13T14:23:25Z",
    "concern": "architecture",
    "tags": ["hosting", "architecture"],
    "milestone": "Architecture & Stack",
    "files_affected": [".context/architecture.md"],
    "summary": {
      "decision_made": "Workers/DOs code lives in src/ (parallel to frontend/ for the Pages SPA). Both are siblings under the project root, reflecting their separate deployment targets — wrangler deploy for src/, wrangler pages deploy for frontend/.",
      "options_considered": [
        "src/ (chosen) — standard Workers project convention, wrangler.toml defaults, and the `src/` vs `frontend/` pairing is immediately understood without explanation",
        "backend/ — symmetric label with frontend/ but over-promises scope (no REST API, no DB, no auth middleware running here)",
        "engine/ — matches the 'game engine' phrase but undersells the Worker HTTP/WS routing and DO orchestration layers"
      ],
      "difficulties_encountered": "None — the decision was straightforward once the question was asked. The architecture doc had been vague about the exact layout.",
      "reversals_and_refactors": "N/A — first time committing the repo layout to a decision document.",
      "key_takeaway": "Naming the Workers directory src/ keeps the project approachable for anyone familiar with Cloudflare Workers conventions, and separates concerns cleanly from the frontend SPA."
    },
    "meta": {
      "confidence_score": 5
    }
  },
  {
    "id": "20260713-mvp-implementation",
    "timestamp": "2026-07-13T23:55:00Z",
    "concern": "code",
    "tags": ["gameplay-loop", "state-machine", "networking", "milestone"],
    "milestone": "MVP Implementation",
    "files_affected": [
      "src/game/types.ts",
      "src/game/random.ts",
      "src/game/setup.ts",
      "src/game/race.ts",
      "src/game/settlement.ts",
      "src/game/machine.ts",
      "src/ws/messages.ts",
      "src/room.ts",
      "src/index.ts",
      "frontend/index.html",
      "frontend/main.ts",
      "frontend/ws-client.ts",
      "wrangler.toml",
      "package.json",
      "tsconfig.json",
      "tests/"
    ],
    "summary": {
      "decision_made": "Delivered full MVP vertical slice: pure-TS game engine (15 state-machine functions), Cloudflare Worker + Durable Object with WebSocket Hibernation API, and vanilla-TS Pages SPA. Race pacing uses self-rescheduling DO alarms (750ms/draw) rather than a synchronous loop — avoids blocking the handler and aligns with the platform's timer idiom used for bid/dist/ready deadlines.",
      "options_considered": [
        "Synchronous race loop in webSocketMessage handler — rejected; would hold the inbound-message invocation open for 60+s and risk duration guards",
        "Self-rescheduling alarms (chosen) — one draw per alarm tick, returns from handler immediately, matches existing bid/dist/ready timer pattern",
        "Room code passed via X-Room-Code header from Worker to DO — DO derives identity from idFromName but needs the human-readable code for state_sync payloads",
        "host_set_track_length WS message added to B1 contract — plan omission caught during implementation; 15-line fix across messages.ts + room.ts"
      ],
      "difficulties_encountered": "Track-card flip / regression rule (Step 5) is the most subtle engine path: 'regression is not a re-passing' means one draw → at most one flip + one regression, no cascading. WebSocket Hibernation API requires compat flag removal (web_socket_hibernation doesn't exist as a flag; hibernation is native in modern Workers). Browser tool sandbox can't reach localhost for D5 smoke — relied on C4 eval smoke (create room → WS → bid → race streaming → settlement) as verification gate.",
      "reversals_and_refactors": "Advisory-driven fix: race pacing redesigned from synchronous loop to alarm-driven before room.ts was written. Room code forwarding added mid-implementation (DO didn't receive the code). collision detection in /init fixed from always-200 to 409-on-collision.",
      "key_takeaway": "A Durable Object game server is a state machine redux in the constructor + alarm() dispatch — the 9-phase loop reads as a switch on room.state with timer-driven transitions. The RNG abstraction (interface + SeededRNG for tests) kept the engine deterministic and fully testable without a Workers runtime."
    },
    "meta": {
      "confidence_score": 4
    }
  },
  {
    "id": "20260714-ui-overhaul-race-pacing",
    "timestamp": "2026-07-14T11:00:00Z",
    "concern": "code",
    "tags": ["gameplay-loop", "ui-ux", "state-machine", "networking", "refactor", "milestone"],
    "milestone": "UI Overhaul + Race Pacing + Host Controls",
    "files_affected": [
      "shared/messages.ts",
      "src/game/types.ts",
      "src/game/machine.ts",
      "src/game/race.ts",
      "src/room.ts",
      "frontend/src/views/HomeView.tsx",
      "frontend/src/views/LobbyView.tsx",
      "frontend/src/views/RacingView.tsx",
      "frontend/src/views/ResultsView.tsx",
      "frontend/src/views/RoomView.tsx",
      "frontend/src/views/DoneView.tsx",
      "frontend/src/styles/global.css",
      "tests/machine.test.ts",
      "tests/race.test.ts"
    ],
    "summary": {
      "decision_made": "Delivered 14 changes from V1 post-playtest UX audit: (1) two-stage race alarm (DRAW/FLIP) with per-room configurable pacing (raceGapDeckMs/raceGapTrackMs, default 2000/1000), replacing hardcoded 750ms constant — gap lands between deck-draw and track-flip so spectators see a breath between moves; (2) SETTLEMENT phase now holds at the race-end snapshot instead of auto-advancing to DISTRIBUTION — host clicks Continue to advance; (3) home screen split into sequential name-then-room flow with sessionStorage persistence; (4) lobby restructured with player list first, inline host bid controls for hosted players, pacing editor, rename-in-place for self and hosted players, Remove button replacing Kick; (5) RacingView replaced absolute-positioned dots with a CSS-grid text track using ♘ knights in per-suit colours (gold Coins, brown Cups, gray Swords, green Clubs); (6) race log grouped into turn-delimited blocks by DECK_DRAW events; (7) new ResultsView shows final track, placements, settlement, player list; (8) DoneView now persists the player list; (9) host_set_bid message added for frontend clarity; (10) change_name and host_set_player_name messages for lobby renames.",
      "options_considered": [
        "Two-stage alarm with private pendingStage field (chosen) — avoids polluting the shared Room type, survives DO eviction between races gracefully, no schema migration needed",
        "Single-stage alarm with an in-room _pendingDraw field — rejected because structuredClone in the game machine would drop it on every edit; plan explicitly called for private DO field",
        "Continuous synchronous race loop — rejected in V1, remains rejected; DO duration guard kills 60s handlers",
        "host_set_bid as a separate message from host_place_bid (chosen) — distinct naming for frontend clarity; identical server handler behind the scenes",
        "Turn-delimited log via a new raceLogGrouped field on Room — rejected; computed at render time from the existing flat raceLog, zero wire cost"
      ],
      "difficulties_encountered": "The ClientMessage union in shared/messages.ts lost host_set_track_length during the edit sequence (a SWAP misaligned with the actual line number because a prior import edit shifted line positions). Caught by tsc and fixed in the same pass. The test file's closing bracket structure similarly misaligned after inserting describe blocks — required a follow-up read-and-repair. The two-stage alarm is the most critical runtime path: if pendingStage desyncs from the actual room state (e.g. eviction mid-race), the next alarm safely restarts at DRAW because the default is null. The lobby rewrite was the largest single component (~300 lines replacing ~100) with nested per-row signals for bid selectors and rename inputs.",
      "reversals_and_refactors": "The plan specified the SETTLEMENT phase not auto-advancing — in V1 it auto-advanced from RACING through SETTLEMENT straight to DISTRIBUTION. The alarm was the tightest integration point: the draw-step alarm sees SETTLEMENT from a 3rd-finish and now returns without scheduling further work. Race pacing removed the last hardcoded timing constant from the DO; every alarm schedule reads from room.raceGapDeckMs or raceGapTrackMs.",
      "key_takeaway": "A DO game server whose core loop is 'draw then flip, each with its own gap' maps naturally to a two-state alarm machine — the drawer function and flipper function are pure, the alarm just picks which one to run and when. The frontend got heavier (SolidJS signals for per-row state in the lobby), but the wire protocol stayed lean: only three new client message types and zero new server message types."
    },
    "meta": {
      "confidence_score": 4
    }
  },
  {
    "id": "20260714-v1-frontend-polish",
    "timestamp": "2026-07-14T17:30:00Z",
    "concern": "code",
    "tags": ["gameplay-loop", "ui-ux", "state-machine", "refactor", "playtest-insight", "bug-hunt"],
    "milestone": "Playtest V1 Review",
    "files_affected": [
      "src/game/types.ts",
      "shared/messages.ts",
      "src/game/machine.ts",
      "src/room.ts",
      "tests/machine.test.ts",
      "tests/race.test.ts",
      "tests/disconnect.test.ts",
      "frontend/src/views/CountdownView.tsx",
      "frontend/src/views/LobbyView.tsx",
      "frontend/src/views/BiddingView.tsx",
      "frontend/src/views/RoomView.tsx",
      "frontend/src/views/RacingView.tsx",
      "frontend/src/views/ResultsView.tsx",
      "frontend/src/ws/handle.ts",
      "frontend/src/styles/global.css",
      ".context/captains_log.json"
    ],
    "summary": {
      "decision_made": "Implemented COUNTDOWN phase between BIDDING and RACING with 3-2-1-GO! display, moved hosted-player bid controls from lobby to bidding view, added start/end columns to track grid, and made horses visually start in 'start' cell and land in 'end' cell when finished. Removed the face-down glyph (U+1F5CC) from the track header row — header cells now show the step number when a track card is not yet flipped and the suit abbreviation in that suit's color when flipped. Fixed the client-side race_log handler to process TRACK_FLIP events and update trackCards[i].isFlipped so the header (and any other component reading the flag) reflects the live flip state.",
      "options_considered": [
        "Overlay countdown on RacingView vs standalone CountdownView (chose standalone for simplicity)",
        "Use isFinished vs position > trackLen for end-cell detection (chose isFinished for correctness with track-flip regressions)",
        "3s vs 4s countdown (chose 4s for one second per tick)",
        "Reuse the in-cell flipped span pattern vs a dedicated header helper for the glyph fix (chose reuse — same class, same abbreviation style, zero new CSS)"
      ],
      "difficulties_encountered": "Coordinating the new COUNTDOWN phase across the state machine, DO alarm handler, and frontend routing required touching many files simultaneously; test files needed startRace() insertion at every placeBid call that expected an auto-advance to RACING. The header glyph appeared correct at first but the client never received isFlipped=true because the race_log handler only processed HORSE_MOVE/HORSE_FINISH events — TRACK_FLIP events updated the server-side room but were never broadcast as a state_sync, leaving the reactive store with all cards permanently face-down.",
      "reversals_and_refactors": "closeBidding previously transitioned directly from BIDDING to RACING. Now it transitions to COUNTDOWN and a new startRace function transitions to RACING when the countdown alarm fires. The frontend was refactored from a monolithic file into separate view components (HomeView, BiddingView, RacingView, ResultsView, DistributionView, DoneView) with a shared WebSocket store.",
      "key_takeaway": "When the server mid-race state lives in a cloned Room but only diff events are broadcast, every event type that mutates server state must have a corresponding client-side reducer. Forgetting one event type (TRACK_FLIP) creates a silent data gap — the DOM never updates because the reactive signal never flips."
    },
    "meta": {
      "confidence_score": 5
    }
  },
  {
    "id": "20260715-distribution-phase-controls",
    "timestamp": "2026-07-15T00:00:00Z",
    "concern": "code",
    "tags": ["gameplay-loop", "state-machine", "networking", "ui-ux", "milestone"],
    "milestone": "Playtest V1 Review",
    "files_affected": [
      ".context/game_design.md",
      ".context/initial_idea.md",
      "shared/messages.ts",
      "src/game/types.ts",
      "src/game/machine.ts",
      "src/room.ts",
      "frontend/src/views/DistributionView.tsx",
      "frontend/src/views/DoneView.tsx",
      "frontend/src/views/LobbyView.tsx",
      "frontend/src/ws/handle.ts",
      "tests/machine.test.ts",
      "tests/messages.test.ts"
    ],
    "summary": {
      "decision_made": "Expanded the Distribution phase from a bare 30s hardcoded auto-distribute timer into a full interactive system: configurable time limit (5s–10min, host-set in lobby), per-player drink assignment UI with host override capability, DRINK_CLEAR race-log events for undo tracking, gaveAll flag on player drinks to signal distribution completion, and endGame() machine function with game_ended server message to reset the room to LOBBY. The frontend DistributionView was rewritten with a player-grid layout showing give/take/consume counters, inline assignment buttons, and a complete-then-ready flow. DoneView was updated with phase badges and drink consumption totals.",
      "options_considered": [
        "Per-player distribution timer vs single global timer (chose single global timer — simpler, host-controlled)",
        "Auto-distribute on timer expiry (chosen) vs force all drinks to be manually assigned (rejected — could block the game if a player disconnects)",
        "gaveAll flag as player signal (chosen) vs host override only (rejected — independent players need self-serve completion)",
        "DRINK_CLEAR event for undo vs full drink state snapshot (chose log event — append-only, matches raceLog pattern)",
        "endGame() resets to LOBBY (chosen) vs full teardown (rejected — keeps room code and player identities for next round)"
      ],
      "difficulties_encountered": "The distribution phase spans both host and independent player actions simultaneously — the host can override any assignment while players self-serve, requiring careful coordination of gaveAll, assignDrink, and clearDrink functions to avoid double-counting. The host_assign_drink and host_clear_drink paths mirror the player paths but skip the gaveAll check since the host overrides the player's ready state. The endGame() function had to also clear the DO alarm to prevent stale race ticks from firing after the game resets.",
      "reversals_and_refactors": "The distribution timer was previously a hardcoded 30s constant in room.ts. It is now a configurable field (distributionTimeLimitMs) on Room with host_set_distribution_time_limit message, validated on the machine layer (5s–600s). The player drink state gained a gaveAll field, the ServerMessage drinks_updated payload carries it, and the client handle function maps it. The endGame function is entirely new — previously there was no way to end a game early.",
      "key_takeaway": "The Distribution phase is the second-most interactive phase after Bidding, and making it fully self-serve with host override requires careful ownership semantics — the machine functions check player identity for permissions, the gaveAll flag gates the ready transition, and the host_ prefixed functions bypass that gate. The DO alarm teardown pattern (storage.deleteAlarm() on endGame) is the same cleanup needed for the host-cancel scenario."
    },
    "meta": {
      "confidence_score": 4
    }
  }
]
